{
  "name": "treninkovy-denik",
  "version": "1.0.0",
  "description": "Tréninkový deník – PWA s Postgres úložištěm, přihlášením a exportem",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.0"
  }
}
